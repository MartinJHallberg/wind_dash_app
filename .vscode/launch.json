{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (from src)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}/src"
        }
    ]
} 