{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run wind_dashapp.app as module",
            "type": "python",
            "request": "launch",
            "module": "wind_dashapp.app",
            "cwd": "${workspaceFolder}/src"
        }
    ]
}